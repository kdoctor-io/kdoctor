{{- if .Values.kdoctorController.podDisruptionBudget.enabled }}
apiVersion: {{ include "capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.kdoctorController.name | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
  {{- if .Values.global.commonAnnotations }}
  annotations:
    {{- include "tplvalues.render" ( dict "value" .Values.global.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
  labels:
    {{- include "project.kdoctorController.labels" . | nindent 4 }}
      {{- if .Values.global.commonLabels }}
      {{- include "tplvalues.render" ( dict "value" .Values.global.commonLabels "context" $ ) | nindent 4 }}
      {{- end }}
spec:
  minAvailable: {{ .Values.kdoctorController.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "project.kdoctorController.selectorLabels" . | nindent 6 }}
{{- end }}
