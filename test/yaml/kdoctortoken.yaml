apiVersion: v1
kind: Secret
metadata:
  name: apiserver-token
  namespace: <<TEST_NAMESPACE>>
  annotations:
    kubernetes.io/service-account.name: kdoctor-controller
type: kubernetes.io/service-account-token